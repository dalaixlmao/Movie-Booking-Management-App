'use client'
import BookingMovieDetail from "@/components/BookingMovieDetail";

export default function () {

//   const session: any = await getServerSession();
//   const userId = Number(session?.user.id);

  return <div><BookingMovieDetail /></div>;
}
